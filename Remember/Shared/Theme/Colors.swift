import SwiftUI

extension Color {
    static let rememberAccent = Color.blue
    static let rememberBackground = Color(.systemBackground)
    static let rememberSecondaryBackground = Color(.secondarySystemBackground)
}
